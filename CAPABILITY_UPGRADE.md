# Capability Upgrade — World Reconstruction Agent 化

2026-09-09。指令は「36候補を評価すること」ではなく「この再構築において、より
強力な World Reconstruction Agent になること」だった。**何を導入するかではなく、
どんな能力を獲得すべきかから逆算**した結果、**候補リストからの導入は0件**、
**自作4件**という結論になった。その理由を残す。

---

## 1. 出発点の実測

コードを書く前に、この環境で**実際に何が動くか**を測った。

| | 状態 |
|---|---|
| Node 22 / Python 3.11 / Blender 4.0.2 / ffmpeg | あり |
| PIL | あり |
| **GDAL / ogr2ogr / QGIS / PDAL** | **無し** |
| **geopandas / rasterio / shapely / pyproj** | **無し** |
| **numpy** | **壊れている**（システム側の競合。`import numpy` が失敗） |
| **OpenCV / scikit-image / scipy** | **無し** |
| GitHub API | **このリポジトリ以外に到達しない**（プロキシのスコープ） |
| GitHub raw / npm / PyPI | 到達する |
| ディスプレイ | 無し（ヘッドレスのみ） |

これが後段の判断のほとんどを決めた。

## 2. Capability Gap

| 能力 | 現状 | 差 |
|---|---|---|
| A. GIS/空間演算 | 独自の投影・タイル取得・MVTデコーダで自足。GDAL級の演算は不要だった | **無し**（この World の規模では） |
| B. 歴史的再構築 | 出典・確度・detail_confidence・年代の突き合わせが稼働中 | 記憶を別枠で持つ場所が無い |
| **C. World の視覚的検査** | **毎回使い捨てのスクリプトを書き、座標を手計算し、PNGを1枚ずつ眺めていた。同じ構図を二度撮れない** | **最大の欠落** |
| D. 知覚QA | データQAのみ。「歩いたらどう見えるか」を測るものが無い | **欠落** |
| E. 北海道農村知識 | 緑町固有の証拠は厚い | 一般論の追加は §16 の禁止事項に触れる |
| F. 記憶の再構築 | 証言が feature の note に埋もれている | **構造が無い** |
| G. Blender自動化 | `--background --python` で3モデルを生成済み・再現可能 | **無し** |
| H. サウンドスケープ | 無音 | 欠落（ただし優先度は低い） |
| I. 性能 | 描画コストを測る手段を今セッションで獲得済み | LOD/streamingは未 |

**C と D が突出していた。** そして候補リスト36件のうち、**C を提供するものは一つも無い。**

## 3. 候補の判定

| 候補 | 実在 | 判定 | 理由 |
|---|---|---|---|
| `nkarasiak/qgis-mcp` | ○ | **REJECT** | QGIS 未インストール・ディスプレイ無し。加えて目的が GIS レイヤ操作で、**Three.js の World を見る能力ではない** |
| `ahujasid/blender-mcp` | ○ | **REJECT** | Blender を対話的に動かすもの。既存の `--background --python` のほうが再現可能でリポジトリに残る。**退行** |
| `isaaccorley/geospatial-skills` | ○ | **REJECT** | geopandas/rasterio 前提。導入不能。かつ独自パイプラインと重複 |
| `opengeos/geoai-skills` | ○ | **REJECT** | 同上。衛星画像DL/物体検出は既に国土地理院タイルで自足 |
| `anthropics/skills` | ○ | **REFERENCE ONLY** | Skill の書き方の参考。中身は本件と無関係 |
| BlenderGIS / blosm | 未検証 | **DEFER** | Blender アドオン。GUI 前提。既存の Blender 経路と重複 |
| 3DTilesRendererJS / PMTiles | 未検証 | **DEFER** | 現状 471 draw calls / 139万三角形。**タイル化が要る規模に達していない**。§18 の順序に従い instancing→LOD が先 |
| GaussianSplats3D / spark (3DGS) | 未検証 | **REJECT（現時点）** | 撮影が要る。**緑町を撮影する手段が無い**。§19 の判定基準を1つも満たせない |
| ODM (photogrammetry) | 未検証 | **REJECT** | 同上。入力写真が存在しない |
| osmnx / geopandas | ○ | **REJECT** | Python GIS スタックが入らない。OSM は Overpass で取得済み |
| tree-gen / modular_tree | 未検証 | **DEFER** | 木は既に1株60三角形。**形ではなく本数が問題**だった（測定済み） |
| Poly Haven / Sonniss / Freesound | ○ | **DEFER** | 汎用アセットの大量配置は §16 の fake fidelity |
| London Charter | ○ | **REFERENCE ONLY → 実装した** | 「成果物には作られ方が付随する」を `evidence/ledger.json` として実体化 |
| 国土地理院各種 / KSJ | ○ | **既に ADOPT 済み** | このセッションで z=12 標高タイルを追加投入 |

**採用0件。** 数を増やすことに価値は無く、**C を埋めるものが存在しなかった**ため。

## 4. 作ったもの

### `scripts/capture-viewpoints.mjs` + `scripts/viewpoints.json`（Priority 1）

定点8つを**名前の付いた地物からの相対位置**で宣言し、撮り、測り、一覧画像に
並べ、基線と比べる。カメラ座標を直書きしないので、構内の寸法が変われば定点も
一緒に動く。

測る量: 空の比 / 植生比 / 輝度 / コントラスト / **エッジ密度（Worldの詰まり）** /
量子化色数 / 地平線の高さ。これらは「真っ黒」「空しか無い」「地面に埋まった」
「単色に潰れた」「何も建っていない」を機械が気づける。

**フレーム時間は測らない。** ソフトウェアラスタライザの数値を携帯の性能として
出すのは捏造になる。描画コストは `measure-budget.mjs` が別に測る。

### `scripts/build-evidence-ledger.mjs` → `evidence/ledger.json`（§11）

全地物を **FACT / INFERENCE / MEMORY** に分類して生成する。手書きではなく生成
なのは、手書きの台帳は必ずデータと乖離し、**乖離した台帳は無い台帳より悪い**から。

現状: 505地物 = FACT 482 / INFERENCE 16 / MEMORY 7。
確度 A:5 B:478 C:22。部分ごとの確度を持つもの386件。
**推定の上に立つ形が13件 — これがこの World の精度の天井。**

### `evidence/memory_atlas.json`（§10）

証言を証拠と**別のファイル**に持つ。混ぜないため。4件、うち1件（ホームの長さ）
が未解決で、**何を訊けば解決するか**まで書いてある。
依頼者の個人的事実は、明示的な指示によりリポジトリに載せていない。

### `.claude/skills/` 2件

- `world-inspection` — 見るループ。WorldInspect の落とし穴（pause 後の frames が
  永久に待つ／centreOf はバウンディングボックス中心／groundY は表面で高さ関数とは別物）を含む。
- `reconstruction-evidence` — FACT/INFERENCE/MEMORY、確度を上げない、誤読を消さずに残す、
  取り込みスクリプトが手作業の同定を消す事故。

`hokkaido-rural-environment` は**作らなかった**。一般論の北海道知識を持つと、
§16 が禁じる「北海道っぽいだけの装飾」を生む方向に働く。緑町固有の証拠のほうが
常に強い。

## 5. この能力が即座に見つけたもの

道具を作った初回の実行で、4件出た。

1. 定点3つの狙いが悪い（自分のミス。修正済み）
2. **2番線の留置車が、ホームから西の林縁の視界を塞ぐ** — 実際そうなので不具合ではない。定点をホーム中央から55m振って回避
3. **`treeline` の植生比 0.042** → 樹冠マスクは方位240°を100%森と言っている。突き合わせたら**木は実際に置かれていた**（60-400mに1,593本）。塞いでいたのは留置車だった
4. **斜里岳が見えない** → 下記

## 6. 斜里岳 — 道具が見つけ、データが直し、残りが記憶に回った

ホームの目の高さから方位72.7°の稜線を測った。

```
手前の稜線(7.3km)      4.34°
斜里岳(17.9km/1477m)   4.30°   → 0.04° 負けて隠れる
```

原因は遠景 DEM が **243m 間隔の点標本**で山頂の錐を落としていたこと。
**稜線は最大値でできている。点標本ではなく最大値を取るべきだった。**

z=12（27.6m）で取得し、出力セルごとに 9×9 の最大値を取るよう変更:

```
最高標高  1,477m → 1,535m   （実測 1,547m まであと12m）
```

再測定:
```
手前の稜線(7.3km)      4.49°   ← 稜線も最大値化で上がった
斜里岳(18.0km/1535m)   4.46°   → まだ 0.03° 負ける
実測1547mでも          4.52° 対 4.49°
```

**差は標高データの分解能より小さい。「見えるかどうか」はこのデータでは決着しない。**

山を見せるために数字を動かすことはしなかった。代わりに、これは
**記憶にしか答えられない問い**として Memory Atlas 側の宿題になった。
DEM の最大値化は、稜線の作り方として単独で正しいので残した。

## 6b. 道具が「直さなくてよい」と教えた例

コンタクトシートを見て「ホーム面が写真より暗い」と思った。描画と写真の画素は
照明が違うので直接は比べられないが、**画面内にある写真由来の基準色（縁端の
青緑、実測で起こした色）との比**を取れば照明が打ち消える。

```
                  ホーム面 / 縁端線（輝度比）
写真(2018)         102.3 / 207.8 = 0.492
描画                99.9 / 191.2 = 0.522    → 差6%
```

**一致していた。印象が誤りだった。** 測る手段が無ければ、ここで根拠なく色を
触っていた。§16 の fake fidelity を1件防いだ形になる。

## 7. 検査

| | 前 | 後 |
|---|---|---|
| `validate-reality-data` | PASS (0 errors, 8 warnings) | PASS (0 errors, 8 warnings) |
| `check-joins` | PASS (793 objects, 0 violations) | PASS |
| `capture-viewpoints` | **存在しなかった** | PASS (8定点, 破綻0) |
| `build-evidence-ledger` | **存在しなかった** | 505地物を分類 |
| 遠景最高標高 | 1,477 m | **1,535 m** |

## 8. 残っている最大のボトルネック

**ホームの長さ。** 測る手を尽くして届かなかった（シームレス写真2m/画素、
1974年空中写真もz=17で限界、レール検出は道床の粒に負けた）。
当事者の記憶が唯一の一次資料で、Memory Atlas に何を訊けば解決するかまで
書いてある。**次に価値が高い能力は、新しいツールではなく、記憶の聞き取りの
具体化。**

## 9. 次に価値が高いもの

1. 記憶の聞き取り（ホーム長・視界・音）の構造化 — 道具は要らない
2. 定点を「季節・時刻」でも撮る — 2010-05-30 は晩春
3. 音（Geophony/Biophony/Anthrophony）。ただし**証拠のある音**から
4. LOD — 三角形の69%がまだ植生。距離での間引きは実装済みだが LOD は未
