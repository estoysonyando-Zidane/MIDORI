{
  "name": "midori",
  "version": "0.1.0",
  "description": "Reality-to-World Engine — a minimal generator that turns real-world geographic data into an explorable 3D World.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --noEmit",
    "validate": "node scripts/validate-reality-data.mjs public/data/worlds/JP_HOKKAIDO_KIYOSATO_MIDORI_20100530",
    "check:joins": "node scripts/dump-world.mjs world-dump.json && node scripts/check-joins.mjs world-dump.json"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "jpeg-js": "^0.4.4",
    "playwright": "^1.49.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
